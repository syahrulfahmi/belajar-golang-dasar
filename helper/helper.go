package helper

/**
access modifier
gunakan huruf besar pada nama fungsi agar bisa di akses di luar package
gunakan huruf kecil untuk access private
*/
func SayHello(name string) string {
	return "Hello " + name
}